import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const TextScreen = () => {
	return(
		<View>
			<Text style={{fontSize: 48}}> TextScreen</Text>
		</View>
	);
};

const styles = StyleSheet.create({

});

export default TextScreen;